export interface IWhoop {
    uNumber: string;
    userId: string;
    name: string;
    body: string;
    utcTick?: number;
}

export interface IUser {
    name: string;
    userPrincipalName: string;
}